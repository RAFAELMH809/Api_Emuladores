services:
  postgres:
    image: postgres:16
    container_name: safeair-postgres
    restart: unless-stopped
    env_file:
      - ./.env
    ports:
      - "6543:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./sql:/docker-entrypoint-initdb.d

volumes:
  postgres_data:
